<div class="row communitySoon">
	<div class="span12">

 {% for product in products %} 

 <div class="item33">
	<div class="item33-photo">
		<a href="{{ path('cs_design_homepage') }}products/{{ product.id }}"> <img
			src="{{ product.image }}" alt="{{ product.name|capitalize }}"
			class="img-rounded" />
		</a>
		<p>{{ product.name|capitalize }}</p>
	</div>
</div>

 {% if loop.index is divisibleby(3) %}
		</div>
</div>
	<div class="row communitySoon">
	<div class="span12">
	<br>
	</br>
{% endif %}

{% endfor %}


	</div>

</div>

	</br>